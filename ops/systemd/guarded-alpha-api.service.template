[Unit]
Description=Guarded Alpha API
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
WorkingDirectory={{PROJECT_DIR}}
Environment=PATH={{PATH}}
ExecStart={{UV_BIN}} run guarded-alpha-api
Restart=on-failure
RestartSec=10

[Install]
WantedBy=default.target
